> **ВНИМАНИЕ: Всегда используй MCP-сервер `fs-seezamonline` для этого проекта. Другие `fs-*` не трогай.**

# AGENTS.md — Seezam Online

> Создано: 8 сентября 2026
> Цель: сохранить контекст проекта для быстрого восстановления между сессиями

---

## 1. Текущее состояние проекта

**Проект:** Личный промо-сайт Алексея Ефимычева
**URL:** https://seezam.online/
**Хостинг:** GitHub Pages (бесплатно)
**Ветка:** `main` → GitHub Actions (сборка из `src/` + `public/`) → Pages

### Стек
- React 18 + Vite 5 + TypeScript
- Tailwind CSS + shadcn/ui
- Framer Motion (анимации), Lucide React (иконки)
- React Router 6 (HashRouter — для GitHub Pages)
- TanStack Query, Zod, Sonner, recharts, date-fns
- ESLint, PostCSS

### Сборка и деплой
- `vite.config.ts` — `base: "./"` (relative paths), порт 8080
- `.github/workflows/deploy.yml` — push на `main` → build `dist/` → Pages
- `.nojekyll` — отключить Jekyll
- `public/404.html` — fallback для hash routing

### Роутинг (HashRouter)
| Путь | Страница |
|------|----------|
| `/` | Index (главная) |
| `/telegram-bots` | Telegram Bots |
| `/mini-apps` | Mini Apps |
| `/web-apps` | Web Apps |
| `/ai-automation` | AI Automation |
| `/cloud-hosting` | Cloud Hosting |
| `/vps-hosting` | VPS Hosting |
| `*` | NotFound |

### Структура
```
seezamonline/
├── src/
│   ├── App.tsx              # HashRouter, Routes, SEO hook
│   ├── main.tsx             # Entry point
│   ├── components/          # Header, HeroSection, Footer, NavLink и shadcn/ui
│   ├── pages/               # 7 страниц + NotFound
│   ├── hooks/               # useSEO
│   └── lib/                 # utils
├── public/                  # статика — робиты, иконки, hero-crop.png, me/
├── dist/                    # билд; в git отслеживается исторически (16 файлов) — см. раздел 4
├── index.html               # HTML-шаблон (gtag.js, meta OG/Twitter)
├── vite.config.ts           # base: "./", port 8080
├── tailwind.config.ts       # Tailwind + shadcn/ui
├── package.json             # зависимости
└── .github/workflows/deploy.yml  # CI/CD
```

### Визитка (Digital Card)
- **Путь:** `public/me/index.html` → https://seezam.online/me/
- **Тип:** чистый HTML + CSS + JS, без фреймворков
- **Контент:** фото, имя "Алексей Ефимычев", "WEB-РАЗРАБОТКА И ДИЗАЙН", кнопки: телефон, WhatsApp, Telegram, сайт, скачать vCard
- **vCard:** `public/me/efimychev.vcf` — Web Share API + fallback скачивание
- **QR-код:** через qrcodejs (CDN)
- **robots.txt внутри визитки:** `User-agent: * Disallow: /` (строка 7 — meta noindex, nofollow)

### Home screen install prompt (получатель)
- **Файл:** `public/me/index.html` — модалка «Сохранить визитку на экран»
- **Механизм:** нативный `navigator.share()` → системный диалог браузера с пунктом «На экран "Домой"» / «Добавить на главный экран»
- **Триггер:** через 5 сек после загрузки, один раз за сессию (`sessionStorage`)
- **Не показывается владельцу:** `isOwner` check
- **Fallback:** если `navigator.share` недоступен — один экран с краткой инструкцией (не пошаговый)
- **Кнопка «Отправить ссылку себе»:** копирует чистую ссылку, пользователь кидает себе в мессенджер
- **Убраны пошаговые инструкции:** были (Шаг 1 → Шаг 2 → Готово), но это пустышки — ничего не происходило
- **UI:** по центру, анимация `install-rise` (scale + translateY), z-index: 101
- **Экраны:** приветствие → нативный диалог шаринга → (опционально) fallback-инструкция → «Понятно»

---

## 2. Что сделано сегодня (8 сентября 2026)

### robots.txt — защита визитки от поиска

**Проблема:** `/me/` (электронная визитка) могла индексироваться поисковиками через главный robots.txt.

**Решение:**
- Добавлен `Disallow: /me/` в `/public/robots.txt` для `User-agent: *`
- Двойная защита: robots.txt (не ходить) + meta-тег `noindex, nofollow` внутри `index.html` (не индексировать)

**Файл:** `public/robots.txt`
```
User-agent: *
Disallow: /me/
Allow: /
```

**Коммит:** `3b61125` — "Disallow /me/ from search engines — protect contact card data"
**Статус:** запушено на `origin/main`, деплой идёт через GitHub Actions.

### Home screen install prompt для получателей

**Проблема:** Получатель открывает визитку, общается, но не может быстро вернуться — нет ярлыка на экране.

**Решение:**
- Кастомная модалка «Сохранить визитку на экран» (не «Установить приложение»!)
- Клик «Сохранить» → `navigator.share({ url })` → системный диалог браузера
- В диалоге реальный пункт: iOS «На экран "Домой"», Android «Добавить на главный экран»
- Показывается через 5 сек, один раз за сессию (`sessionStorage`)
- Владелец (`?owner=1`) модалку не видит
- Если share API недоступен — один экран с краткой инструкцией

**Коммиты:**
- `c1731e4` — "Add home screen install prompt for vCard visitors" (первая версия с инструкциями)
- `6cd76c4` — "Replace instructions with native share dialog for home screen install" (замена на нативный шаринг)

---

## 3. Что осталось / следующие шаги

### Приоритетные
- [x] Деплой home screen install модалки прошёл успешно
- [ ] Убедиться, что визитка не появилась в Google Search Console

### Текущее состояние
- [x] Нативный share-диалог работает на большинстве устройств
- [x] Модалка не показывается владельцу (`?owner=1`)
- [x] Один раз за сессию (`sessionStorage`)
- [x] GitHub Pages деплой работает, скрипты выполняются

### Возможные улучшения (не начато)
- [ ] Протестировать на Samsung (меню ⋮ → «Установить приложение» или «Добавить на экран "Домой"»)
- [ ] Протестировать на Xiaomi (меню ⋮ → «Добавить» → «Экран "Домой"» — два шага)
- [ ] Добавить конкретные инструкции по бренду (определяем Samsung/Xiaomi → показываем точный путь)
- [ ] Сделать кнопку «Отправить ссылку себе» более заметной (если человек не нашёл пункт в меню)
- [ ] SEO-оптимизация главной страницы (meta-теги на всех страницах)
- [ ] Добавление Open Graph изображений (сейчас hero-crop.png 245x245 — маленький)
- [ ] Добавление sitemap.xml
- [ ] Проверка доступности (a11y) компонентов shadcn/ui
- [ ] Аналитика: gtag.js уже подключён в `index.html` — проверить сбор данных

---

## 4. Состояние репозитория и деплоя — вывод от 12.09.2026

### Проверено и работает — не трогать

Конвейер: **push в `main` → GitHub Actions → свежая сборка из `src/` + `public/` → Pages**.

- Подтверждено практикой: несколько пушей подряд, Actions срабатывает, прод обновляется.
- `dist/` в git на прод **не влияет** — workflow всегда собирает заново из исходников.
- Правки в `public/me/` (визитка) подхватываются сборкой напрямую: Vite копирует `public/` в артефакт как есть.

### Что лежит «не в порядке», но безвредно

| Объект | Фактическое состояние | Влияние на прод | Срочность |
|--------|----------------------|-----------------|-----------|
| Ветка `gh-pages` | не обновлялась с июня 2026 | нулевое — прод отдаёт контент из сборки `main` | удалять незачем |
| `dist/` в git | отслеживается 16 файлов; в `.gitignore` закомментарен ложный текст «dist/ is committed to gh-pages branch» | нулевое | шум в `git status`, не более |
| Source в Settings → Pages | в UI подсвечено `gh-pages / (root)` | фактически публикует workflow | **не переключать** |

Последняя строка — намеренное решение: источник не переключать и ветку не удалять, пока всё работает. Риск нулевой пользы против риска уронить рабочий деплой.

### Триггеры: когда наведение порядка действительно нужно

Порядок нужен не «когда-нибудь» и не ради чистоты, а по конкретным симптомам:

1. **Главный сигнал.** Запушены код + изменённый `dist/`, и при этом прод не совпадает с тем, что в репозитории → начинается поиск причины. Тогда нужно первым делом проверить, **успешен ли последний run Actions**, а не анализировать отличия в `dist/`. Изменения в `dist/` — это ложный след: они выглядят как причина, но на прод не влияют.
2. **Шум мешает работать:** правки в `dist/` постоянно попадают в коммиты, `git stash` / `git checkout` задевают собранные ассеты, возникают конфликты при merges на хеш-именах (`index-<hash>.js`). Сейчас этого нет.
3. **Меняется способ публикации** (другой хостинг, ручной деплой) — тогда `dist/` надо привести в соответствие или удалить осознанно.

### Смысл уборки (зачем, когда пробьёт час)

Убрать `dist/` из отслеживания нужно не для красоты, а чтобы:
- `git status` показывал только реальные изменения исходников;
- пропал сам соблазн коммитить собранное и искать причину расхождений не там.

Процедура (один коммит, на деплой не влияет):
```bash
git rm -r --cached dist          # убрать из индекса, файлы останутся на диске
echo "dist/" >> .gitignore       # плюс поправить ложный комментарий про gh-pages в .gitignore
git commit -m "Stop tracking dist/ build output"
git push
```

### Что уже сделано по должности (12.09.2026)

- Коммит `441333c` — `TITLE` в `public/me/efimychev.vcf`: `ПРОДЮСЕР IT ПРОЕКТОВ` → `WEB-РАЗРАБОТКА И ДИЗАЙН`. Запушено, Actions отработал, прод обновлён.
- Должность в визитке **в 3 местах**, при правке синхронизировать вручную:
  1. `public/me/efimychev.vcf` — строка `TITLE`
  2. `public/me/index.html:489` — `<div class="role">`
  3. `public/me/index.html:601` — JS-строка `var vcf = "BEGIN:VCARD…TITLE:…"` (vCard для шаринга)
- На 12.09 все три значения совпадают: `WEB-РАЗРАБОТКА И ДИЗАЙН`.

### Ограничение предыдущей сессии

Проверки через `curl` и GitHub API подвешивались (сеть недоступна) — часть выводов по `gh-pages` и Source получена из кэша и скриншота настроек, а не живыми запросами. Не повторять эти проверки вслепую; при сомнениях смотреть Actions и Settings → Pages в браузере.

---

## 5. Важные решения и ограничения

### Техническая архитектура

| Решение | Обоснование |
|---------|-------------|
| `HashRouter` вместо `BrowserRouter` | GitHub Pages не поддерживает серверный роутинг |
| `base: "./"` в vite.config.ts | Относительные пути для GitHub Pages |
| `public/404.html` | Fallback для hash routing на GitHub Pages |
| `.nojekyll` | Отключить Jekyll на GitHub Pages |
| Порт 8080 | Dev server port |

### CI/CD — GitHub Actions

**Файл:** `.github/workflows/deploy.yml`

**Триггер:** push на `main` (автоматически)

**Пайплайн:**
1. `npm ci` — установка зависимостей
2. `npm run build` — production build в `dist/`
3. `actions/upload-pages-artifact@v3` — загрузка артефакта `dist/`
4. `actions/deploy-pages@v4` — деплой на GitHub Pages

**Особенности:**
- `.github/workflows/deploy.yml` в git — деплой автоматический
- `dist/` в репозитории — исторический остаток, в деплое не участвует (подробно — раздел 4)
- После push на `main` — деплой запускается сам, обычно 2-3 минуты
- Проверить статус: https://github.com/seezam/seezamonline/actions
- `NODE_ENV: production` при билде
- Node.js 20, кэш npm
- Permissions: contents, pages, id-token

**Локально:** `npm run build` — результат в `dist/`

### Визитка — разделение режимов

Механизм `?owner=1` — два взгляда на одну страницу:

```
seezam.online/me/              → визитка без инструментов (получатель)
seezam.online/me/?owner=1      → визитка + QR + копирование ссылки (владелец)
```

- Один HTML-файл, нулевая серверная логика
- `isOwner = new URLSearchParams(window.location.search).get('owner') === '1'`
- При `isOwner`: показывается `#qr-block` (QR + кнопка копирования) и `#owner-tag` ("режим владельца")
- QR всегда кодирует "чистую" ссылку (без `?owner=1`) — чтобы сканирующие попадали в режим получателя
- Копирование ссылки — тоже чистая ссылка (без параметров)

### Home screen install — ограничения

| Ограничение | Детали |
|-------------|--------|
| Без SW | Только нативный диалог шаринга, без PWA-промпта |
| Android | Пункт называется по-разному: Samsung «Установить приложение», Xiaomi «Добавить → Экран "Домой"», Pixel «Добавить на главный экран» |
| iOS Safari | Только через «Поделиться → На экран "Домой"», нет нативного промпта |
| Один раз за сессию | `sessionStorage` — при повторном открытии в новой вкладке модалка появится снова |

### Визитка — разделение режимов (обновлено 9.09.2026)

**Режим определяется ТОЛЬКО по `?owner=1` в URL:**

```
seezam.online/me/              → визитка без инструментов (получатель)
seezam.online/me/?owner=1      → визитка + QR + копирование ссылки (владелец)
```

- `localStorage` и `sessionStorage` **убраны** (коммит `d908e28`) — они «прилипали» ко всему браузеру и получатель видел QR владельца
- PWA-ярлык любого пользователя (владельца или получателя) открывает **режим получателя** — это известный компромисс
- Чтобы владелец видел инструменты: либо закладка с `?owner=1`, либо вручную добавить параметр в URL
- `start_url` в `manifest.json` = `"./"` (без параметров) — чтобы PWA-ярлык получателя не открывал режим владельца

### Проблема кэширования PWA

- Chrome PWA кэширует файлы намертво — при установке ярлыка старая версия сохраняется локально
- На новых устройствах кеша нет — всё работает корректно
- На устройствах с установленным PWA: очистка кеша Chrome (Настройки → Приложения → Chrome → Хранилище → Очистить данные) или режим инкогнито
- Для избежания кэширования в будущем можно добавить query-строку к файлам (`index.html?v=2`) — но это усложнит сборку

### Контакты (жестко захардкожены)

| Параметр | Значение |
|----------|----------|
| Имя | Алексей Ефимычев |
| Должность | WEB-РАЗРАБОТКА И ДИЗАЙН (было: ПРОДЮСЕР IT ПРОЕКТОВ — до 12.09.2026) |
| Телефон | +7 916 023-76-77 |
| WhatsApp | +79160237677 |
| Telegram | @seezam |
| Сайт | https://seezam.online |

### Контекст не сохраняется между сессиями

- Это AGENTS.md — единственный источник восстановления контекста
- При возврате: `git status` → `git log --oneline -5` → изучить файл
- `.env` не в git, не трогать без разрешения

---

## 6. Полезные ссылки

| Ресурс | URL |
|--------|-----|
| Сайт | https://seezam.online/ |
| Визитка | https://seezam.online/me/ |
| Демо-бот | @seezam_demo_bot |
| GitHub | https://github.com/seezam/seezamonline |
| Telegram | https://t.me/seezam |
| Email | hello@seezam.online |

---

## 7. Команды

```bash
cd /home/al/dev/seezamonline
npm run dev       # Dev server (port 8080)
npm run build     # Production build
npm run preview   # Preview production build
git status        # Проверить состояние
git log --oneline -5  # Последние коммиты
```

---

*Обновлено: 12 сентября 2026 — должность в vCard (коммит `441333c`); зафиксировано состояние репозитория: что работает, что является безвредным шумом и при каких симптомах пора наводить порядок (раздел 4)*
